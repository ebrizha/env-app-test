apiVersion: v1
kind: Service
metadata:
  name: react-app-test
spec:
  type: NodePort
  selector:
    app: react-app-test
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
